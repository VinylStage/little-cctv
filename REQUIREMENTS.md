# little-cctv — 요구사항 & 아키텍처 (컨펌용)

> 상태: **개발 전 컨펌 대기**. 아래 §7 확인 항목 답변 + 전체 컨펌 후 개발 시작.
> 기술 근거: 웹 조사 기반(신뢰도 표기). 명령/설정 예시는 빌드 시 실측 검증 예정.

## 1. 목적 / 범위
- Razer Kiyo Pro(이 Mac에 USB 연결)를 임시 CCTV로 사용. 기간: 하루 정도.
- 외부(인터넷)에서 브라우저로 접속해 실시간 시청.
- 외부 노출 / IP 제한 / 포트포워딩은 **사용자가 공유기에서 직접 처리**. 별도 인증·보안 앱 구현 없음.

## 2. 확정 요구사항
- 실시간 스트리밍 (약간의 지연 허용)
- 영상 + 오디오(Kiyo Pro 내장 마이크) **동시 송출**
- **로컬 녹화** (이 Mac 디스크에 저장)
- **움직임 감지**
- 인증 없음

## 3. 추천 아키텍처 (신뢰도 高)

스택: **ffmpeg(avfoundation 캡처) → MediaMTX → 브라우저 WebRTC + 로컬 녹화 + 모션 스크립트(RTSP 구독)**

```
Kiyo Pro (USB)
  └─ ffmpeg 캡처/인코딩 (H264 baseline, B프레임 0 + Opus 48k)
       └─ RTSP → MediaMTX ─┬─ WebRTC 재생   (브라우저 http://<mac>:8889/cam)
                           ├─ 로컬 녹화     (fMP4, 디스크)
                           └─ RTSP 재배포   → 모션 감지 스크립트 (스냅샷 + 이벤트 로그)
```

**왜 이 조합인가**
- MediaMTX 단일 바이너리가 서브초 WebRTC 재생 + 로컬 녹화를 **기본 내장** → 녹화용 별도 부품 불필요, 내장 브라우저 플레이어 제공(프론트 최소).
- 대안 비교: go2rtc(녹화·모션 없음 → 부품 2개 추가) / OBS(브라우저 재생 위해 결국 MediaMTX 필요, 모션 없음) / Frigate(유일한 올인원이나 macOS에서 Docker USB 패스스루 불가·CPU 바운드 → 1일용 비현실적).

## 4. 컴포넌트
1. **MediaMTX** — 코어 서버. WebRTC(:8889), RTSP(:8554), 녹화 재생 API(:9996). config = `mediamtx.yml`(인증 미설정, 코멘트 없이 별도 문서화).
2. **ffmpeg 캡처** — MediaMTX가 `runOnInit`로 **상시 기동**(녹화가 항상 돌아야 하므로 `runOnDemand` 아님). Kiyo Pro 영상+마이크 → H264 baseline + Opus 48k → RTSP publish.
3. **모션 감지** — `rtsp://localhost:8554/cam` 구독(**카메라 재오픈 금지**).
   - 1단계: ffmpeg `scene` 필터로 스냅샷 + 타임스탬프 로그(추가 의존성 0).
   - 오탐 많으면 2단계: Python + OpenCV 프레임 차분(min-area 게이트)로 교체.
4. **접속** — 브라우저 `http://<mac-ip>:8889/cam`. 외부는 사용자가 포트포워딩.

## 5. 핵심 기술 제약 / 주의 (개발 시 반영)
- macOS USB 웹캠은 대체로 **한 프로세스만** 장치 오픈 가능 → 단일 캡처 후 팬아웃 필수. 모션은 RTSP에서 읽음. *(신뢰도 中)*
- WebRTC 오디오는 **Opus 강제**. AAC 넣으면 브라우저에서 무음. *(高)*
- H264 **B프레임 0(baseline)** 필수. *(高)*
- macOS **TCC 권한**: 카메라+마이크 권한은 실행 '터미널 앱'에 부여됨. 백그라운드 기동 시 프롬프트가 안 떠 조용히 실패 → 최초 1회 Terminal 포그라운드 실행으로 승인. *(中)*
- **모델 종속**: 원본 Kiyo Pro는 1080p60 최대, 1440p/4K 없음. 4K/1440p는 Kiyo Pro **Ultra**(별도 모델). 해상도값이 모델에 종속됨. *(高)*
- **원격 WebRTC 주의**: 포트포워딩만으로 부족할 수 있음. WebRTC 미디어는 ICE 포트(기본 8189/udp,tcp)도 열어야 하고, MediaMTX에 공인 IP를 advertise(`webrtcAdditionalHosts`)해야 원격 재생이 붙음. **8889만 열면 신호는 되는데 영상이 안 붙는** 상황 가능. *(中, 빌드 시 실측 확인)*

## 6. 기본값 제안 (컨펌/수정 대상)
- **해상도/프레임**: 1080p30 (~4Mbps). 60fps는 대역폭 2배 → 감시용은 30 권장.
- **녹화**: 상시 녹화, fMP4, 60초 세그먼트, 24시간 후 자동 삭제(디스크 보호). 720p 기준 하루 수십 GB.
- **모션 동작**: 스냅샷 저장 + 타임스탬프 이벤트 로그만(녹화는 상시라 별도 트리거 없음).
- **웹페이지**: MediaMTX 내장 플레이어 사용(가장 빠름). 원하면 얇은 커스텀 랜딩(라이브+녹화목록 링크) 추가 가능.

## 7. 컨펌 전 확인 필요
1. ★ Q4에서 고른 **"Something else"**가 구체적으로 뭐야? (위 4개 외 추가 기능이 있는지)
2. ★ 카메라 정확한 모델: **원본 Kiyo Pro(1080p60)** vs **Kiyo Pro Ultra(4K/1440p)**? → 해상도 확정에 필요(연결돼 있으니 빌드 때 자동 확인도 가능).
3. 모션 감지 수준: 로그/스냅샷이면 충분? 아니면 "움직임 있을 때만 녹화" 또는 사람/객체 인식까지?
4. 해상도/프레임 기본값(**1080p30**) 괜찮아?
5. 웹페이지: **MediaMTX 내장 플레이어**로 충분? 커스텀 페이지 원해?

## 8. 문서화 범위
- 1일 임시 도구라 `ARCHITECTURE.md`/`ROADMAP.md`/`IMPLEMENTATION_AUDIT.md` 3종은 생략하고, 이 문서 + `README`(실행법)만 둘 예정. 정식 문서 필요하면 말해줘.
